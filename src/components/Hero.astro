---
import HeroTerminal from './HeroTerminal.astro';

const GITHUB_URL = 'https://github.com/galvaniclabs/oxide';
const FOUNDING_DOC_URL = 'https://github.com/galvaniclabs/oxide/blob/main/FOUNDING_DOC.md';
---

<section class="min-h-screen flex items-center pt-14">
  <div class="max-w-6xl mx-auto px-6 w-full py-16 md:py-24">
    <div class="reveal">
      <!-- What Oxide IS — clear, confident -->
      <h1 class="text-[clamp(2.2rem,5vw,4.2rem)] font-extrabold uppercase leading-[1.05] tracking-[-0.03em]">
        THE IDE YOUR<br />
        <span class="text-oxide-accent">AI AGENT</span><br />
        CAN OPERATE.
      </h1>

      <!-- Emotional hook — name the pain, then resolve -->
      <p class="mt-6 text-oxide-text-mid text-base md:text-lg max-w-3xl leading-relaxed">
        Stop switching windows. Stop copy-pasting context.<br />
        Your agent doesn't just edit files — it <span class="text-oxide-text font-medium">operates your debugger.</span><br />
        <span class="text-oxide-text font-medium">You and your agent, one terminal, one toolchain.</span>
      </p>

      <!-- Product grounding — what it actually is -->
      <p class="mt-4 text-base md:text-lg text-oxide-text-dim max-w-xl leading-relaxed">
        Terminal-native. Familiar keybindings. VSCode extensions. Built in Rust.
      </p>

      <div class="mt-8 flex flex-wrap gap-4">
        <a
          href={GITHUB_URL}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 bg-oxide-accent text-oxide-bg font-semibold text-sm px-6 py-3 rounded hover:shadow-accent-glow-lg hover:bg-oxide-accent-bright transition-all"
        >
          Star on GitHub &#9733;
        </a>
        <a
          href={FOUNDING_DOC_URL}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 border border-oxide-border text-oxide-text-mid font-medium text-sm px-6 py-3 rounded hover:border-oxide-accent hover:text-oxide-text hover:bg-oxide-hover transition-all"
        >
          Founding doc &rarr;
        </a>
      </div>
    </div>

    <div class="mt-12 reveal">
      <HeroTerminal />
    </div>
  </div>
</section>
